import React, { Component } from "react";
import "./Footer.js.css"

/**
 * @class Footer is the footer at the bottom of each page.
 */
class Footer extends Component{
    /**
     * Renders the footer, showing it on the screen.
     * @returns {JSX.Element}
     */
    render(){
        return(
            <div id={"footer"}/>
        )
    }
}
export default Footer;